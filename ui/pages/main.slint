export component Main {
        Text {
        text: "Sheepit Manager!";
        color: white;
        font-size: 30px;
        x: root.width / 2 - (self.width / 2);
        y: root.height - root.height + 30px;
    }
}